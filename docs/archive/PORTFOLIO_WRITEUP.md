# Archived AI Agent Portfolio Writeup

This file replaces the older `ai-agent/docs/PORTFOLIO_WRITEUP.md` as an archive marker.

The original writeup was a useful narrative snapshot of the AI Agent architecture, but it predated the current Prompt Registry, Responsible AI, CI, and platform docs. Keeping it in the active docs set made the public GitHub view look less current because it referenced older service counts, test totals, and cost estimates.

Use the current public docs instead:

- `ai-agent/README.md` for the AI Agent overview
- `ai-agent/docs/WORKFLOWS.md` for LangGraph workflow details
- `ai-agent/docs/OBSERVABILITY.md` for tracing and RAI observability
- Root `README.md` and `docs/ARCHITECTURE.md` for the platform story

Historical details remain available in git history if needed.
