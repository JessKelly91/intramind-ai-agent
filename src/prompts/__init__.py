"""Versioned prompt registry for the IntraMind AI agent.

Import prompts and helpers from :mod:`prompts.registry`. See that module's
docstring for the change workflow (edit template -> bump version -> update
locks.json -> changelog).
"""
