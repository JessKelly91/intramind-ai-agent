# Core AI/ML frameworks
langgraph>=0.2.0
langgraph-checkpoint-sqlite>=1.0.0  # Conversation memory/persistence
aiosqlite>=0.20.0  # Async SQLite for LangGraph checkpointing
langchain>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0
langchain-anthropic>=0.2.0

# LLM Integrations
openai>=1.0.0
anthropic>=0.39.0

# HTTP Client for API Gateway
httpx>=0.27.0
aiohttp>=3.10.0

# Configuration & Environment
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# CLI Interface
click>=8.1.0
rich>=13.0.0
prompt-toolkit>=3.0.0

# Multimodal Document Processing
pypdf>=3.17.0  # Modern PDF parsing (replaces PyPDF2)
pdfplumber>=0.11.0
python-docx>=1.1.0  # Word document parsing
pytesseract>=0.3.10
python-pptx>=1.0.0
Pillow>=10.0.0
python-magic>=0.4.27; platform_system != "Windows"  # File type detection on Linux/macOS
python-magic-bin>=0.4.14; platform_system == "Windows"  # Bundled libmagic for Windows
chardet>=5.2.0  # Encoding detection for text files
langchain-text-splitters>=0.2.0  # Advanced text chunking

# Utilities
tenacity>=8.0.0  # Retry logic
structlog>=24.0.0  # Structured logging

# Observability / Responsible AI tracing (Step 1: Phoenix)
arize-phoenix-otel>=0.6.0
openinference-instrumentation-langchain>=0.1.0

# RAG evaluation (Step 2: Ragas)
ragas>=0.2.0
langchain-ollama>=0.2.0
datasets>=2.19.0

# PII detection / redaction (Step 3: Presidio)
# After install, run: python -m spacy download en_core_web_lg
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0

# Drift reporting (Step 5: Evidently)
evidently>=0.4.30
pandas>=2.0.0

# Development & Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0
pytest-cov>=5.0.0
black>=24.0.0
ruff>=0.6.0
mypy>=1.11.0

# Optional: Local LLM support (Ollama)
ollama>=0.3.0
